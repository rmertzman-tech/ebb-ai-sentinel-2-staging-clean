--- SENTINEL-6A/score_review.py
+++ SENTINEL-7A/score_review.py
@@ -1,12 +1,12 @@
 #!/usr/bin/env python3
-"""Join locked SENTINEL-6A blind review with the hidden key and compute the RC2.4 release gate."""
+"""Join locked SENTINEL-7A blind review with the hidden key and compute the RC2.5 release gate."""
 import argparse,csv,json
 from pathlib import Path
 
 p=argparse.ArgumentParser()
 p.add_argument('--reviewed',required=True)
 p.add_argument('--key',required=True)
-p.add_argument('--out',default='EBB-AI-SENTINEL-6A_REVIEW_SCORE.json')
+p.add_argument('--out',default='EBB-AI-SENTINEL-7A_REVIEW_SCORE.json')
 a=p.parse_args()
 
 def rows(path):
@@ -22,8 +22,8 @@
     if k:
         x=dict(r);x.update(k);joined.append(x)
 
-CAND='candidate_rc2_4'
-BASE='baseline_rc2_3'
+CAND='candidate_rc2_5'
+BASE='baseline_rc2_4'
 candidate=[x for x in joined if x.get('version')==CAND]
 baseline=[x for x in joined if x.get('version')==BASE]
 primary=[x for x in candidate if str(x.get('trial'))=='1']
